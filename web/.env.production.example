# Copy to web/.env.production on the server BEFORE running `npm run build`.
# NEXT_PUBLIC_* values are baked into the client bundle at build time — changing
# this afterwards requires a rebuild, not just a restart.

NEXT_PUBLIC_API_URL=https://wedding.peculiex.com
INTERNAL_API_URL=http://127.0.0.1:4100
